# 交接记录（Handoff）

## 交接摘要
- 目标：支持 `Voicemeeter8Setup.exe` 作为驱动安装输入。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 在设置页选择 `Voicemeeter8Setup.exe` 执行安装
  2. 安装完成后重新检测驱动（必要时重启）
- 风险与注意事项：Setup.exe 的静默参数/重启策略由安装包自身决定，脚本仅负责拉起并记录退出码。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：直接在应用里选择 `Voicemeeter8Setup.exe` 点击“安装驱动”，把新日志贴回。

## 交接摘要
- 目标：解决“安装日志成功但驱动仍未检测到”的体验问题。
- 当前进度：已完成脚本策略优化。
- 剩余任务（按优先级）：
  1. 安装后执行一次系统重启
  2. 重启后在应用内重新“检测驱动”
- 风险与注意事项：VB-CABLE 官方说明要求安装后重启；未重启时设备枚举可能为空。
- 验证现状（已通过/未通过）：已通过（脚本可自动走 VB-CABLE 官方安装器并完成执行）。
- 建议下一条指令：重启后返回设置页点击“检测驱动”，若仍未检测到再贴最新 `install-virtual-audio-driver.last.log`。

## 交接摘要
- 目标：安装脚本自动提权，并支持多个 INF 文件中手动选择安装。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 提供目标虚拟声卡 INF 文件并执行安装验证
  2. 可选：安装后自动触发设备重枚举提示（部分驱动需要重启音频服务）
- 风险与注意事项：脚本会触发 UAC 提权；若取消提权或 INF 未签名，安装会失败并返回具体错误。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：将 INF 放到 `drivers/windows/` 后，在设置页选择具体 INF 再点“安装驱动”。

## 交接摘要
- 目标：修复虚拟驱动安装脚本报错（ParserError）。
- 当前进度：已完成。
- 剩余任务（按优先级）：
  1. 放置真实驱动 INF 到 `drivers/windows/` 后再次执行安装
- 风险与注意事项：当前脚本已能正常解析与执行；若仍安装失败，优先检查 INF/签名/管理员权限。
- 验证现状（已通过/未通过）：已通过（脚本无语法错误；当前仅因缺少 INF 退出）。
- 建议下一条指令：提供驱动 INF，我可继续帮你把安装结果与设备枚举联动验证跑通。

## 交接摘要
- 目标：继续“接入虚拟音频驱动”，增加应用内安装与检测闭环。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 放置可用虚拟声卡驱动 INF 到 `drivers/windows/`（当前脚本会自动选第一个 INF）
  2. 若要对外发布，补驱动签名与安装器打包流程
- 风险与注意事项：当前是“驱动安装接入链路”而非“本仓库自产内核驱动”；安装依赖管理员权限和系统驱动策略。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：先把驱动 INF 放入 `drivers/windows/`，然后在设置页点击“安装驱动”并执行“检测驱动”。

## 交接摘要
- 目标：接入虚拟音频驱动。
- 当前进度：已完成“已安装驱动优先接入”并通过构建。
- 剩余任务（按优先级）：
  1. 若要实现“系统级注册声卡”，需新增驱动安装与签名分发链路（当前仓库尚未包含）
  2. 可选：设置页增加“已检测到的虚拟驱动设备”列表与手动重绑定
- 风险与注意事项：当前实现是对系统内已安装虚拟驱动设备的优先识别和自动绑定，不等价于应用自身注册内核级虚拟声卡。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：安装虚拟驱动后执行“应用虚拟端点数量 -> 检查端点默认绑定 -> 连线路由”回归。

## 交接摘要
- 目标：修复“虚拟端点创建没有效果”。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在设置页增加端点“已绑定到哪个真实设备”的只读列表，便于确认自动绑定结果
- 风险与注意事项：本次修复将虚拟端点默认设为启用并自动绑定可用设备；若系统当前无可用输入或输出设备，端点仍可创建但会保持未绑定状态。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：在设置里将虚拟输入/输出数改为 2/2 并应用，然后在设备卡绑定下拉中选择 `v-in-*` / `v-out-*` 做路由手测。

## 交接摘要
- 目标：在窗口中间显示工程名，支持点击编辑，并统一使用 `.ck` 后缀。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：增加“重命名后自动保存”开关（当前仍保持显式保存策略）
- 风险与注意事项：编辑时会自动去掉用户输入的 `.ck` 后缀再入库，展示层始终追加 `.ck`，避免双后缀。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：启动 `npm run tauri dev`，点击标题栏中部工程名改名并保存，确认工程列表中名称同步更新。

## 交接摘要
- 目标：将“工程列表”从设置页迁移到画布顶部菜单。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：清理 `SettingsPanel.css` 中历史工程列表样式残留（仅样式清洁，不影响功能）
- 风险与注意事项：工程加载入口已迁移到顶部菜单；设置页不再提供工程列表页签，避免入口分散。
- 验证现状（已通过/未通过）：已通过（`npm run build`，无 CSS 语法 warning）。
- 建议下一条指令：运行 `npm run tauri dev`，手测顶部菜单工程列表的展开、当前工程高亮与加载切换。

## 交接摘要
- 目标：去掉画布虚拟创建菜单，改为设置中按需创建/删除 In/Out 虚拟端点。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：虚拟端点页增加“当前绑定到哪个真实设备”的明细表
- 风险与注意事项：当前是应用层固定端点池管理，不是驱动级系统声卡注册；但已支持数量创建/删除和统一命名规则。
- 验证现状（已通过/未通过）：已通过（`npm run build`、`cargo build`）。
- 建议下一条指令：在设置页调整 In/Out 数量后，使用原有设备卡绑定下拉选择 `v-in-*`/`v-out-*` 完成路由。

## 交接摘要
- 目标：让“虚拟输入/虚拟输出”点击后真正创建可见可用的固定虚拟端点。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在设置页增加固定端点池可视化（显示 `v-in-*` / `v-out-*` 占用情况）
- 风险与注意事项：当前虚拟按钮会优先启用第一个空闲固定端点，并自动绑定到可用真实设备；若无真实设备则端点可见但路由启动会提示未绑定。
- 验证现状（已通过/未通过）：已通过（`npm run build`、`cargo build`）。
- 建议下一条指令：连续点击虚拟输入/输出按钮两次，确认生成不同固定端点并可参与路由。

## 交接摘要
- 目标：按“固定端点 + 动态映射”方案直接实现后端能力。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 前端增加固定虚拟端点管理页（启用/重命名/绑定）
  2. 前端在创建虚拟卡片时优先使用 `v-in-*`/`v-out-*`
- 风险与注意事项：当前为应用层固定端点映射，不是驱动层系统注册声卡；系统音频设置中不会新增真实声卡设备。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：先让我把设置页接上 `list_virtual_endpoints/update_virtual_endpoint`，完成端到端可视化配置。

## 交接摘要
- 目标：允许虚拟设备ID后缀手动自定义，并支持恢复为跟随名称。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在设备卡片上增加后缀锁定图标提示
- 风险与注意事项：后缀输入会进行 slug 规范化（空格转 `-`，去掉特殊符号）；前缀 `core-link-vdev-` 固定不变。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：针对虚拟设备执行“改名后缀自动变化→手动改后缀锁定→点跟随名称恢复”的回归手测。

## 交接摘要
- 目标：设备卡也显示命名模式；自动命名标签更友好；虚拟设备ID支持“前缀+名称后缀”绑定。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：将虚拟设备ID后缀生成规则做可配置（拼音/slug策略）
- 风险与注意事项：当前无法在系统层“安装”新虚拟声卡驱动；本次实现的是 Core Link 内部虚拟设备标识与命名绑定。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：新建虚拟输入/输出后分别改名，核验 ID 后缀联动与卡片命名模式显示。

## 交接摘要
- 目标：将设备命名模式（自动/自定义）可视化显示在名称输入区域。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：将状态标签同步显示到设备卡片头部
- 风险与注意事项：标签仅反映 `nameCustomized` 状态，不改变既有命名逻辑。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：执行“改名→重置→切换绑定”回归，确认标签与行为一致。

## 交接摘要
- 目标：实现设备名称“自动跟随绑定”与“手动自定义锁定”并支持重置。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在 UI 上显示“自动/自定义”状态标签
- 风险与注意事项：`nameCustomized` 字段用于控制跟随逻辑；旧工程未包含该字段时默认按“未自定义”处理。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：按“改名→切换绑定→重置名称→再次切换绑定”的路径做一次手测验收。

## 交接摘要
- 目标：让虚拟设备可自主创建，并能通过统一前缀判断是否由 Core Link 创建。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在工程导出信息中显示虚拟设备前缀统计
- 风险与注意事项：`core-link-vdev-*` 为应用内虚拟节点标识，不等价于系统驱动层虚拟声卡。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：创建虚拟输入/输出后观察卡片来源标签与绑定字段是否都显示为 Core Link 虚拟设备。

## 交接摘要
- 目标：修复设备卡片名称与实际绑定设备不同步。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：增加“名称锁定”开关（锁定后切换绑定不自动改名）
- 风险与注意事项：当前会在切换绑定时自动改名，并保留原 `#序号` 后缀。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：在卡片下拉和属性面板中分别切换绑定设备，确认名称同步一致。

## 交接摘要
- 目标：修复“虚拟输入/虚拟输出按钮不可点击”。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在按钮旁增加轻量状态点（已检测到虚拟声卡/回退模式）
- 风险与注意事项：当前无虚拟声卡时会创建回退虚拟节点（可编辑/可连线），但无法直接承接系统级虚拟声卡音频。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：在你的运行环境点击两个虚拟按钮确认可创建，再决定是否要添加状态点提示。

## 交接摘要
- 目标：去掉设备来源中已过时的“虚拟设备”开关。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：将 `isVirtual` 字段进一步收敛，仅用于处理器类节点
- 风险与注意事项：当前虚拟路由能力以“绑定到系统虚拟声卡”实现，不再依赖属性面板手动切换旧开关。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：手测输入/输出卡片绑定切换与虚拟声卡串流链路。

## 交接摘要
- 目标：支持虚拟输入/输出设备卡片，用于外部播放器选择虚拟播放设备后进行串流控制。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在设置面板增加“虚拟声卡检测结果”列表
  2. 可选：增加一键“虚拟输入 -> 物理输出”自动连线
- 风险与注意事项：当前能力基于系统中已安装的虚拟声卡（如 VB-CABLE/VoiceMeeter）；应用本身不创建内核级虚拟音频驱动。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：把播放器输出切到虚拟声卡后，在工具栏点击“虚拟输入”并连接到目标输出做端到端验证。

## 交接摘要
- 目标：实现无限画布体验，避免拖拽到边缘后出现边界问题。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：将重定位阈值 `safeMargin` 做成可配置项
  2. 可选：重定位发生时增加开发态调试开关（线上默认关闭）
- 风险与注意事项：重定位会整体平移设备世界坐标，但视觉位置保持不变；该过程不写入历史，避免污染撤销栈。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：在高缩放与低缩放下长距离拖动画布，确认无限平移体验稳定。

## 交接摘要
- 目标：让自动排版以画布中心为参考，避免布局总贴上方。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：进一步按当前缩放级别动态调整列间距/行间距
  2. 可选：增加“自动排版后自动适配视图”开关
- 风险与注意事项：当前是“视图中心锚点布局”，并未改动世界坐标尺寸（仍为大画布模式，非数学意义无限画布）。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：在不同平移位置点击自动排版，确认设备簇稳定落在当前视野中心。

## 交接摘要
- 目标：修复回采设备卡片颜色、把新增面板并入画布左上纵向工具栏、创建设备落点改为画布中心、设备选择并入设备卡片。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：工具栏按设备类型补充文本标签（当前为图标+tooltip）
  2. 可选：设备切换后是否自动重命名卡片（当前保持原名称）
- 风险与注意事项：输入卡片会根据绑定设备 ID 自动判断是否为 loopback 并切换紫色样式；若系统无回采设备，工具栏“回采”按钮会禁用。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：运行 `npm run tauri dev`，在不同缩放与平移位置下新增设备，确认都落在当前可视中心；再在卡片下拉切换输入/回采设备验证颜色变化。

## 交接摘要
- 目标：回采设备独立标注与分组；修复工程保存/重启丢失；设置新增工程列表页可加载。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：工程重命名/删除能力
  2. 可选：工程列表搜索与固定排序规则
- 风险与注意事项：当前工程“保存”会覆盖当前活动工程；无活动工程时自动创建新工程。
- 验证现状（已通过/未通过）：已通过（`npm run build`、`cargo build`）。
- 建议下一条指令：重启应用后在设置-工程列表中验证加载切换与自动恢复行为。

## 交接摘要
- 目标：解决“扬声器监控不到”和“初始化后需重加设备”问题。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 若部分机器 loopback 失败，增加 UI 级错误提示与降级说明
  2. 可选：把启动绑定修正改为可配置开关
- 风险与注意事项：扬声器回采依赖系统回采能力；少数设备驱动可能不支持。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：在输入列表中选择“(扬声器回采)”并连线测试，再重启应用验证自动恢复。

## 交接摘要
- 目标：让电平更灵敏且刻度更合理，并修复输出链串流能力。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 若需更精准音量感知，可补峰值保持与 dB 标尺映射
  2. 若需“真实输出回采串流”，需增加 WASAPI loopback 路径
- 风险与注意事项：当前输出->输出串流本质是上游输入源扇出到多个输出，不等同于回采输出硬件信号。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：在 1 输入 2 输出链路下做实际听感与电平联动验证。

## 交接摘要
- 目标：优化采样率不一致下的系统级路由稳定性与延迟。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 若要更高音质，替换为更高阶重采样（如窗口化 sinc）
  2. 增加运行时可调的目标延迟（当前固定约 45ms）
  3. 加入统计监控（欠载/过载计数）用于自动调参
- 风险与注意事项：线性重采样优先保证稳定性和实时性，不是 Hi-Fi 级转换。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：运行 `npm run tauri dev` 后用不同采样率输入/输出设备做压力测试。

## 交接摘要
- 目标：在系统级执行层基础上继续增强为多路并发路由。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 加入采样率不一致时的重采样策略（当前依赖设备默认配置，兼容性有限）
  2. 增加路由级开关/优先级，避免复杂拓扑自动下发冲突
  3. 如需达到 ASIOLinkPro 级别，继续实现系统虚拟声卡驱动层
- 风险与注意事项：多路并发目前是“按合法连线去重后并发启动”，未做高级时钟同步与补偿。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：运行 `npm run tauri dev`，用两组本机 input->output 连线进行并发听感测试。

## 交接摘要
- 目标：按“直接做系统级”实现后端音频执行层，而非仅前端模拟。
- 当前进度：已完成并通过双端构建（`cargo build`、`npm run build`）。
- 剩余任务（按优先级）：
  1. 真实设备联调（不同采样率/声道组合）并评估缓冲策略
  2. 扩展为多路并发路由（当前优先取第一条本机 input->output 连线）
  3. 若需 ASIOLinkPro 级能力，新增系统级虚拟声卡驱动/内核层组件
- 风险与注意事项：当前实现为系统设备直通引擎，不会在系统里注册“虚拟音频设备”。
- 验证现状（已通过/未通过）：已通过（`cargo build`、`npm run build`）。
- 建议下一条指令：运行 `npm run tauri dev` 并执行“本机输入→本机输出连线”实测，我可继续做延迟与稳定性优化。

## 交接摘要
- 目标：让“真实麦克风输入”真正可用，并补齐工程保存/恢复核心闭环。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 在目标机器实测 `setSinkId` 对多输出设备切换是否稳定
  2. 若要达到 ASIOLinkPro 级系统路由，补 Tauri/Rust 音频引擎与系统级虚拟声卡
- 风险与注意事项：WebView 不支持 `setSinkId` 时会自动回退系统默认输出，不影响基本监听。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：运行 `npm run tauri dev` 后执行“输入→自动布线→保存→重开恢复”全链路手测。

## 交接摘要
- 目标：接入真实电平，并让虚拟设备具备可串流语义。
- 当前进度：已完成首版实现并通过构建。
- 剩余任务（按优先级）：
  1. 手测不同系统输入设备切换后的实时采集稳定性
  2. 如需系统级虚拟声卡，再扩展 Tauri/Rust 后端音频链路
- 风险与注意事项：当前“虚拟设备”为应用内虚拟总线语义，不是操作系统级可见虚拟声卡。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：运行 `npm run tauri dev` 后进行真实语音输入路由联调，我可根据结果继续调参或补系统级能力。

## 交接摘要
- 目标：实现输入/输出设备核心控制能力。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：增加“重置为默认参数”按钮
  2. 可选：把控制变更同步写入本地工程快照
- 风险与注意事项：通道数减少会删除超范围连线（符合当前规则）。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：若需要，我可继续补“输入监听/输出监听开关”。

## 交接摘要
- 目标：添加设备编组（Group）能力。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在设备卡上显示组标识（如 G1）
  2. 可选：增加“仅解绑单个设备”操作
- 风险与注意事项：当前取消编组会清除“选中设备所在的整个组”。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：若你需要，我可继续补“组可视标签 + 右键组操作”。

## 交接摘要
- 目标：添加单设备快捷多选操作（Ctrl+点击）。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：增加视觉提示“当前为多选模式”
  2. 可选：空白处 Ctrl+点击保留选择
- 风险与注意事项：Mac 下使用 Meta（⌘）点击同样生效。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：如果需要，我可以继续加“框选后编组（Group）”。

## 交接摘要
- 目标：继续完善多选操作效率（全选与清空）。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：状态栏显示当前选中数量
  2. 可选：支持 Ctrl+点击设备加入/移出选择
- 风险与注意事项：当前 Ctrl+A 会将第一个设备设为主选中设备。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：若需要我可继续补“Ctrl+点击单设备多选”。

## 交接摘要
- 目标：继续增加多选能力（Shift 增量选择）。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：增加 `Ctrl+A` 全选设备
  2. 可选：多选状态栏显示“已选 N 项”
- 风险与注意事项：当前 Ctrl 与 Shift 都可触发框选，Shift 为追加模式，Ctrl 为覆盖模式。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：如果需要，我可以继续做 `Ctrl+A` 与“Esc 清空选择”。

## 交接摘要
- 目标：增加 Ctrl+框选批量移动。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：支持 Shift 增量框选
  2. 可选：批量移动时显示“已选 N 个设备”提示
- 风险与注意事项：批量拖拽当前由设备 header 触发，不支持在卡片非 header 区域起拖。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：如需我可继续补“Shift 多选 + Ctrl+A 全选”。

## 交接摘要
- 目标：增加画布滚轮缩放交互。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：增加 `Ctrl+滚轮` 快速缩放模式
  2. 可选：缩放时显示临时缩放百分比浮层
- 风险与注意事项：当前滚轮缩放速度固定，如需更细腻可调 `zoomDelta`。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：如需我可继续加“缩放动画过渡”。

## 交接摘要
- 目标：继续提升连线提示可读性（重名设备消歧）。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：按设备类型附加后缀（如 麦克风(input)#1）
  2. 可选：为 tooltip 增加弱化副标题显示完整设备 ID
- 风险与注意事项：`#序号` 基于当前同名设备顺序，删除设备后序号会重新排列。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：如需固定序号，可改为创建时持久化别名。

## 交接摘要
- 目标：将连线提示升级为“设备名 + 通道号”。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：设备重名时追加类型后缀（input/output）
  2. 可选：提示中加入信号强度百分比
- 风险与注意事项：设备名来自当前节点状态，重命名后提示会实时变化。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：若需要我可继续加“设备重名消歧显示”。

## 交接摘要
- 目标：增加连线端口编号悬浮提示（如 OUT1 → IN1）。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 可选：在提示中追加设备名（例如 麦克风 OUT1 → 扬声器 IN1）
  2. 可选：选中连线后常驻显示映射标签
- 风险与注意事项：提示位置采用鼠标坐标偏移，超小窗口下可能接近边缘。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：如果你需要，我可以把提示内容升级为“设备名 + 通道号”。

## 交接摘要
- 目标：修复连线仍未对齐的问题。
- 当前进度：已完成（锚点计算补齐通道区域顶部内边距）。
- 剩余任务（按优先级）：
  1. 可选：为端口中心绘制开发态对齐点（仅调试）
  2. 可选：不同 DPI 下做视觉回归截图
- 风险与注意事项：后续改 `device-channels` padding 需要同步 `AUDIO_DEVICE_CARD_LAYOUT.channelsPaddingTop`。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：若仍有偏差，请给出当前缩放比例与窗口分辨率，我可继续做像素级校准。

## 交接摘要
- 目标：继续提升连线稳定性并修复端口不对齐。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 实机手测高 DPI/不同缩放下端口中心对齐
  2. 如需可继续加入“吸附前预览磁力线”
- 风险与注意事项：设备卡任意高度改动需同步 `AUDIO_DEVICE_CARD_LAYOUT` 常量。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：补“连线端口编号提示 + 通道映射编辑”。

## 交接摘要
- 目标：加上连线右键删除，并修复连线与端口不对齐。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 在真实使用分辨率下手测不同缩放比例的端口对齐
  2. 评估是否加“吸附到最近端口”的连线辅助
- 风险与注意事项：后续若调整设备卡高度，需同步 `AUDIO_DEVICE_CARD_LAYOUT` 常量。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：继续加“连线端点吸附 + 端口命中扩大”。

## 交接摘要
- 目标：加上“连线选中删除 + 非法目标高亮”交互。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 桌面端手测 Delete/Backspace 删除连线行为
  2. 评估是否增加“选中连线右键菜单删除”作为替代操作
- 风险与注意事项：当前连线删除为键盘触发，若需要纯鼠标流程可在下一轮补右键菜单。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：继续补充“连线右键删除 + hover 高亮路径”。

## 交接摘要
- 目标：修复连线不对应/闪烁、设备卡布局、画布拖拽规则、自动布线与适应视图定位。
- 当前进度：已完成并通过构建。
- 剩余任务（按优先级）：
  1. 执行桌面端手测（设备拖拽、连线、适应视图、定位按钮）
  2. 若需可继续补充“连线可点击删除/高亮非法目标”
- 风险与注意事项：连线端口坐标依赖 `AUDIO_DEVICE_CARD_LAYOUT`，后续改卡片结构需同步常量。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：基于实际使用截图继续微调设备卡与属性面板密度。

## 交接摘要
- 目标：将设备创建改为三类入口并修复设备卡、画布、拖拽、连线体验。
- 当前进度：已完成并通过 `npm run build`。
- 剩余任务（按优先级）：
  1. 在桌面端运行验证本机设备枚举标签是否稳定显示
  2. 继续细化自动布线策略（processor 中间链路）
- 风险与注意事项：未授权麦克风权限时，系统可能返回匿名设备名（如“输入设备 1”）。
- 验证现状（已通过/未通过）：已通过（编译构建）。
- 建议下一条指令：请按实际使用场景给出“设备卡视觉细节”偏好（更扁平或更立体）后继续细化。

## 交接摘要
- 目标：将界面与交互调整为 Material Design + 圆角卡片风格，并修复偏离预期的功能行为。
- 当前进度：已完成。
- 剩余任务（按优先级）：
  1. 运行 `npm run tauri dev` 检查桌面环境下窗口拖拽与标题栏按钮行为
  2. 根据实际产品预期微调具体配色与间距 token（如需）
- 风险与注意事项：自动布线当前为 MVP 版本（输入到输出按序映射），复杂拓扑仍需后续策略扩展。
- 验证现状（已通过/未通过）：已通过（`npm run build`）。
- 建议下一条指令：继续完善自动布线策略（支持 processor 节点链路）。

## 交接摘要
- 目标：创建一套目录与工作约定，降低 Agent 会话中断后的恢复成本。
- 当前进度：已创建 `docs/agent-memory` 目录与 4 类记忆工件模板。
- 剩余任务（按优先级）：
  1. 后续任务按状态机持续更新 `session-log.md`
  2. 出现关键取舍时补充 `decision-log.md`
  3. 每次结束任务前刷新本文件摘要
- 风险与注意事项：若只更新聊天不更新文档，会再次出现记忆丢失风险。
- 验证现状（已通过/未通过）：已通过（文件已创建，可直接使用）。
- 建议下一条指令：基于当前需求开启下一项功能实现，并按模板更新会话快照。

## 模板

```md
## 交接摘要
- 目标：
- 当前进度：
- 剩余任务（按优先级）：
- 风险与注意事项：
- 验证现状（已通过/未通过）：
- 建议下一条指令：
```
